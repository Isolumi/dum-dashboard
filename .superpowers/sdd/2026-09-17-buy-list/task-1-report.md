# Task 1 report: Buy list

## Scope and result

Implemented the complete name-only Buy list slice in `/Users/isolumi/Documents/CS/dum-dashboard/.worktrees/discord-job-saves`, on `codex/buy-list`.

Read the Task 1 brief first. Then read the approved design, `AGENTS.md`, and the existing uncommitted plan. Used the TDD, Supabase, frontend-design, React best-practices, systematic-debugging, browser-testing, and verification skills. The approved design and existing Jobs controls set the UI. No new palette, library, runtime dependency, or fields were added.

No subagents were used. No production SQL, push, PR, merge, or deployment was done. Local Docker was not started. No existing user records or unrelated feature behavior were changed. The parent owns database verification and release. The existing plan is included in the implementation commit.

## Implementation

- Additive table: UUID ID, checked trimmed name with 1–300 characters, creation timestamp, descending creation-time/ID index, RLS, denied direct API roles, and service-role CRUD grants.
- Strict Zod inputs and typed canonical records. Server functions reuse owner/environment, no-store, and same-origin write checks. Database and thrown transport/configuration errors become safe domain errors. Missing rename/delete records return safe not-found errors.
- List reads use ordered 1,000-row pages rather than a fixed list limit.
- Controller: initial load, retry, retained last-good refresh data, 10-second polling, optimistic add/rename/delete, pending IDs, boolean mutation results, independent item writes, and item-only rollback. Temporary IDs and duplicate pending-item writes are blocked. Reads are invalidated at mutation start and end, including failures. Reads begun during pending writes cannot replace optimistic state. Timestamp sorting compares actual instants, then IDs.
- Shared list: keyboard add, inline Enter/Escape rename, focus restoration, retained failed drafts, wrapping left-aligned names, compact hover/focus/touch delete controls, and readable loading/empty/error states. The compact list has a bounded scroll area. Both views support all actions.
- Registry: ShoppingBag entry directly after Calendar. Overview: Buy list directly below Calendar in the primary column. Existing side ranks and side ordering remain unchanged. Route tree is generated by TanStack tooling and the required build, never edited by hand.

## RED/GREEN evidence

All commands below ran from the requested worktree. SQL tests were left for the parent, as required.

### Initial RED

Command:

```sh
bun run test src/routes/buy-list src/routes/_layout/buy-list src/routes/_layout/-OverviewPage.test.tsx
```

Exit 1. Six test files failed. Five new suites could not import their absent schema/domain/server/controller/component modules. The two new Overview assertions failed because the Buy list registry entry and card were absent. Nine existing Overview tests passed. These were the expected absent-feature failures requested in the brief.

Tests were written before implementation. They cover name limits/trimming/strict inputs, UUIDs, canonical CRUD queries, all list pages, safe errors, owner/origin/no-store ordering, optimistic writes, concurrent item-only rollback, stale reads, duplicate writes, temporary IDs, retry, retained refresh data, polling, keyboard and draft behavior, wrapping, delete visibility classes, empty states, and Overview/registry placement.

### Domain/server GREEN

```sh
bun run test src/routes/buy-list
```

Exit 0. Three files and 29 tests passed: five schema, 12 domain, and 12 server-boundary tests.

Migration creation:

```sh
bunx supabase migration new --help
bunx supabase migration new create_buy_list_items
```

Both exited 0. The CLI created `supabase/migrations/20260917161829_create_buy_list_items.sql`. SQL edits used `apply_patch`. The pgTAP file was written before the migration SQL was filled in. No database command was executed by this implementer.

`bunx supabase --version` confirmed CLI 2.117.0. Consulted current [Supabase select documentation](https://supabase.com/docs/reference/javascript/select), [RLS documentation](https://supabase.com/docs/guides/database/postgres/row-level-security), and the changelog before implementation. The relevant access boundary uses grants plus RLS, with the existing server-only service-role client.

### UI/controller iterations

- The first combined UI/controller run had 67 passing tests and two failures. The failure came from an `it.each` table spreading array entries instead of passing an items array. Corrected the test table; no production change was needed. The next run passed all 69 tests in six files.
- Added a focus-restoration assertion before its fix. `bun run test src/routes/_layout/buy-list/-BuyList.test.tsx` exited 1: two Escape/focus tests failed and 12 tests passed. The input closed but focus stayed on the body. Moved restoration to the post-render effect. The component tests then passed.
- Added tests that use the real controller and shared list in both page/card views. Their initial run reached optimistic temporary/pending rows before the write finished. Those controls correctly rejected clicks. Corrected the tests to wait for canonical enabled controls. The combined run then passed 71 tests in seven files.
- Extended stale-read tests to create/rename/delete before and during writes, failure-end invalidation, a successful independent create during failed rename, and rejected invalid/missing writes. `bun run test src/routes/_layout/buy-list/-useBuyListController.test.tsx` exited 0: 22 tests passed.
- Self-review found that string timestamp comparison can misorder timezone formats. Added the regression before the fix. The controller command exited 1: the new timestamp test failed and 22 passed. Changed the comparison to `Date.parse` with the ID tie-breaker.

Final focused command:

```sh
bun run test src/routes/buy-list src/routes/_layout/buy-list src/routes/_layout/-OverviewPage.test.tsx
```

Exit 0. Seven files and 79 tests passed: five schema, 12 domain, 12 server-boundary, 23 controller, 14 shared-component, two real-controller view, and 11 Overview tests.

Focused lint:

```sh
bunx oxlint src/routes/buy-list src/routes/_layout/buy-list src/routes/_layout/index.tsx src/routes/_layout/-OverviewPage.test.tsx src/tools/registry.ts vitest.config.ts src/lib/database.types.ts
```

Exit 0. No warnings or errors.

## Local browser checks

Chrome DevTools tools were not available. Used the Playwright CLI skill as the fallback. A temporary local Vite fixture imported the actual shared list, controller, page, card, controls, and stylesheet. Server-function imports were replaced with an in-memory API. Environment loading was pointed at a nonexistent test-only directory. No Supabase call or real user record was involved.

Commands used `bash /Users/isolumi/.codex/skills/playwright/scripts/playwright_cli.sh --session buy-list ...` and the separate `buy-list-mobile` session. Ran `open`, `snapshot`, `fill`, `press Enter`, `click`, `press Escape`, read-only `eval`, `console`, `requests`, and `screenshot`. The mobile session used `open ... --mobile`.

- Actual Enter added an item in the card and in the dedicated page.
- Enter saved a rename, and delete removed the item in both views.
- Escape restored focus to the name button. Confirmed the active element was `Rename Fixture item 1` after cancellation.
- Card title opened the page. The page showed its level-one heading and existing narrow page shell.
- Mobile viewport: 360 pixels wide; hover and fine-pointer media queries were false; delete opacity was 1.
- Compact list: 320-pixel client height, 1,539-pixel scroll height on mobile. Long-name control was 96 pixels high and wrapped. No horizontal document overflow.
- Inspected the mobile screenshot. Names, delete controls, and add form stayed inside the card.
- Final desktop and mobile console checks: zero errors and zero warnings. Initial Vite dependency optimization produced three transient 504 responses, then reloaded with the new dependencies. The final checks were clean.

Browser evidence remains in the ignored task directory, including `.playwright-cli/page-2026-09-17T16-24-34-477Z.png`. Closed both test browser sessions and stopped the fixture server. Removed only the four temporary fixture source files. They are not part of the commit. The CLI reference used the old `network` command name; command discovery showed `requests`, which was used instead.

## Database verification

Parent-reported evidence in this task:

- New migration passed in isolated PostgreSQL 17.
- Independent actual-role checks denied all CRUD for anon/authenticated and passed service-role CRUD, trimming/length checks, and retained creation timestamp. The parent used rollback.
- All 30 standard pgTAP tests passed after the parent installed official pgTAP v1.3.4 in the isolated pod.

This implementer did not run these database checks or connect to the confirmed production project. No production change is included in the verification claim.

## Full verification before commit

The required verification phase ran on the final implementation.

- `bun run test`: exited 132; the runner stopped with `SIGILL` before test results. Read current macOS crash reports for Bun/Node. They confirm illegal-instruction termination in the full runner. Local versions: Node v26.8.2, Bun 1.3.14. The underlying runtime cause was not established. No runtime or test configuration was changed to hide it.
- First `bun run test --maxWorkers=2 --minWorkers=1`: exit 1. All 84 files ran: 82 passed and two failed; 1,292 tests passed and two failed. One was the Docker-backed monitoring test. The other was the old Monies registry assertion requiring immediate adjacency to Calendar. The approved insertion requires Buy list between Calendar and Monies. Updated only that registry assertion to check Calendar → Buy list → Monies → Jobs → Homelab. No Monies component or behavior was changed. A final full-suite retry uses the same two-worker command, with no excluded tests; its result is recorded below.
- `bun run lint`: exit 0. No warnings or errors across 214 files.
- `bun run build`: exit 0. Client, SSR, and Nitro outputs completed. Route generation included `/buy-list`. Build emitted CSS optimizer, large-chunk, and dependency-unused-import warnings. No unrelated source was changed to address those warnings.
- `bun run fmt:check`: exit 1. Only `scripts/verify-monitoring-values.ts` and `tests/-verify-monitoring-values.test.ts` failed. Both are unchanged from HEAD. Left them unchanged to preserve scope.
- Scoped formatting check: exit 0. All 19 initially scoped files were correctly formatted. `git diff --check` exited 0. The small Monies registry assertion update receives its own focused test/format/lint checks below.

Final registry/feature check:

```sh
bun run test src/routes/_layout/monies/-MoniesBentoCard.test.tsx src/routes/buy-list src/routes/_layout/buy-list src/routes/_layout/-OverviewPage.test.tsx
bunx oxfmt --check src/routes/_layout/monies/-MoniesBentoCard.test.tsx
bunx oxlint src/routes/_layout/monies/-MoniesBentoCard.test.tsx
```

All exited 0. Eight test files and 91 tests passed. The registry-only edit was correctly formatted and had no lint warning or error.

Final complete suite:

```sh
bun run test --maxWorkers=2 --minWorkers=1
```

Exit 1 after a complete 36.19-second run. All 84 files ran: 83 passed and one failed. All 1,294 tests ran: 1,293 passed and one failed. There were no Buy list, registry, or other application test failures.

Exact blocked existing test:

```text
tests/-verify-monitoring-values.test.ts
monitoring values contract mutations > rejects a rendered Ingress in addition to the approved Grafana Ingress
docker: Cannot connect to the Docker daemon at unix:///Users/isolumi/.docker/run/docker.sock. Is the docker daemon running?
```

The test expects Helm-render verification output. Docker stopped before that verification could run. Left the test and monitoring code unchanged. Did not start Docker or exclude any test. Fresh CI must run this check.

Report formatting check:

```sh
bunx oxfmt --check --ignore-path /dev/null .superpowers/sdd/2026-09-17-buy-list/task-1-report.md
```

Exit 0 after the final evidence update. The final staged diff check also exited 0. The commit contains 24 scoped files.

## Changed files

- `docs/superpowers/plans/2026-09-17-buy-list.md` — pre-existing uncommitted plan, included as requested.
- `supabase/migrations/20260917161829_create_buy_list_items.sql`.
- `supabase/tests/database/buy_list_items.test.sql`.
- `src/lib/database.types.ts`.
- `src/routes/buy-list/buy-list.schemas.ts`.
- `src/routes/buy-list/buy-list.domain.ts`.
- `src/routes/buy-list/buy-list.functions.ts`.
- `src/routes/buy-list/-buy-list.schemas.test.ts`.
- `src/routes/buy-list/-buy-list.domain.test.ts`.
- `src/routes/buy-list/-buy-list.functions.test.ts`.
- `src/routes/_layout/buy-list/-useBuyListController.ts`.
- `src/routes/_layout/buy-list/-BuyList.tsx`.
- `src/routes/_layout/buy-list/-BuyListBentoCard.tsx`.
- `src/routes/_layout/buy-list/index.tsx`.
- `src/routes/_layout/buy-list/-useBuyListController.test.tsx`.
- `src/routes/_layout/buy-list/-BuyList.test.tsx`.
- `src/routes/_layout/buy-list/-views.test.tsx`.
- `src/routes/_layout/-OverviewPage.test.tsx` — includes registry checks.
- `src/routes/_layout/monies/-MoniesBentoCard.test.tsx` — registry assertion only; accounts for the approved Buy list insertion without changing Monies tests or code otherwise.
- `src/routes/_layout/index.tsx`.
- `src/tools/registry.ts`.
- `src/routeTree.gen.ts` — generated only.
- `vitest.config.ts` — new server test included/excluded in the correct projects.
- `.superpowers/sdd/2026-09-17-buy-list/task-1-report.md` — this report.

## Self-review and concerns

Compared the complete diff and new files with the brief and approved design. Checked server-only admin access, generic error handling, strict validation, canonical rows, unchanged creation time on rename, full-list pagination, item-only rollback, independent writes, temporary/pending ID protection, read invalidation on both outcomes, keyboard/focus/draft behavior, both views, wrapping/touch controls, bounded card height, registry placement, and unchanged side order. No remaining feature defect was found in this self-review.

Outstanding verification concerns: the existing Docker-backed monitoring test cannot run with Docker stopped; two unrelated baseline files fail the full formatting check; the initial default-worker full run crashed. Fresh CI and the parent's independent whole-branch review remain required. Production migration and live release checks remain with the parent. No release action was taken.

Commit subject: `feat(buy-list): add complete Buy list slice`.

## Review fix round 1 — preserve timestamp microseconds

The Important review item is confirmed. `Date.parse` discards fractional digits after milliseconds. Thus `2026-09-17T12:00:00.000900Z` and `2026-09-17T12:00:00.000100Z` were treated as equal, and the UUID tie-breaker placed the older item first. This also changed an already database-sorted response.

Used the code-review, systematic-debugging, TDD, React best-practices, and verification skills. Added the failing tests before changing production code. The regression uses the later timestamp with UUID `11111111-1111-4111-8111-111111111111` and the earlier timestamp with UUID `22222222-2222-4222-8222-222222222222`. It checks the initial database-sorted response and a reversed refresh response. A second case checks equivalent timezone offsets and shortened fractional output. Kept the existing timezone regression unchanged.

RED command:

```sh
bun run test src/routes/_layout/buy-list/-useBuyListController.test.tsx --maxWorkers=2 --minWorkers=1
```

Exit 1: 2 new microsecond-order tests failed; 23 existing tests passed. The expected first item was `.000900Z` with the lower UUID; the received first item was `.000100Z` with the higher UUID. Both UTC and different-timezone cases exposed the same precision loss.

The fix compares timezone-normalized milliseconds first, then the remaining microseconds, then UUID descending. Fractional digits are right-padded to six digits, so `.0009` and `.000900` retain the same database precision. Whole epoch microseconds are not stored in a JavaScript number, which avoids unsafe-integer precision loss. The fraction expression is hoisted to module scope per the React performance guidance. No mutation, polling, or rollback logic changed.

GREEN used the same controller command. Exit 0: 25 tests passed. Then added an equal-microsecond-instant test with different timezone and fractional-width formats to verify that UUID descending is used only for a true timestamp tie.

Repeated the standalone controller command after final formatting and the extra tie test. Exit 0: 1 file and all 26 tests passed.

Final covering command:

```sh
bun run test src/routes/_layout/buy-list src/routes/_layout/-OverviewPage.test.tsx --maxWorkers=2 --minWorkers=1
```

Exit 0: 4 files and 53 tests passed: controller 26, shared UI 14, page/card views 2, Overview 11. This includes the retained timezone regression and all new precision cases.

Lint and scoped formatting commands:

```sh
bun run lint
bunx oxfmt src/routes/_layout/buy-list/-useBuyListController.ts src/routes/_layout/buy-list/-useBuyListController.test.tsx
bunx oxfmt --check src/routes/_layout/buy-list/-useBuyListController.ts src/routes/_layout/buy-list/-useBuyListController.test.tsx
bunx oxfmt --check --ignore-path /dev/null .superpowers/sdd/2026-09-17-buy-list/task-1-report.md
git diff --check
```

Lint exited 0 with 0 warnings and 0 errors across 214 files. The two-file formatter and format check exited 0. The Node tools printed the existing `DEP0205` runtime deprecation notice. The report format and diff checks also exited 0.

Self-review: the comparator preserves all six database fractional digits, normalizes timezone offsets, handles omitted or shortened fractional digits, and keeps the ID tie-breaker for equal instants. The new tests exercise the real controller output, not a comparator mock. Only the controller, its test file, and this report changed. No migration, dependency, generated route, other feature, or existing user data changed. No subagents, Docker start, database command, external write, or release action was used. The parent owns push and scoped re-review. The earlier whole-suite concerns remain recorded above; this fix did not rerun or claim to clear them.

Fix commit subject: `fix(buy-list): preserve timestamp microsecond ordering`.
