---
phase: quick
plan: 260402-1a5
subsystem: todos-ui
tags: [calendar, date-picker, popover, layout, shadcn]
dependency_graph:
  requires: []
  provides: [calendar-date-picker, max-width-layout]
  affects: [AddTodoRow, TodoRow, TodosPage]
tech_stack:
  added: [react-day-picker, date-fns]
  patterns: [shadcn-calendar-popover, base-ui-render-prop]
key_files:
  created:
    - src/components/ui/calendar.tsx
  modified:
    - src/components/ui/button.tsx
    - src/routes/_layout/todos/-AddTodoRow.tsx
    - src/routes/_layout/todos/-TodoRow.tsx
    - src/routes/_layout/todos/index.tsx
key_decisions:
  - shadcn Calendar component installed via CLI; "use client" directive is harmless in TanStack Start (already present in other components)
  - TodoRow calendar icon uses opacity-0/group-hover:opacity-100 pattern (matches delete button) for consistent hover behavior
  - Priority button Tab key: removed explicit dateInputRef focus (no longer valid); Tab now flows naturally to the Calendar trigger button
metrics:
  duration: 122s
  completed: "2026-04-02"
  tasks_completed: 2
  files_changed: 5
---

# Quick 260402-1a5: Todo Page Calendar Popup for Date Input — Summary

**One-liner:** Replaced native HTML date inputs in AddTodoRow and TodoRow with shadcn Calendar popovers (react-day-picker), added max-w-3xl centered layout to todo page, and fixed name input padding.

## What Was Done

### Task 1: Install shadcn Calendar component and dependencies (747f414)
- Ran `bunx --bun shadcn@latest add calendar --overwrite`
- Created `src/components/ui/calendar.tsx` with full shadcn base-nova styling
- Updated `src/components/ui/button.tsx` (minor lg size variant tweak from CLI)
- Added `react-day-picker@^9.14.0` and `date-fns@^4.1.0` to dependencies

### Task 2: Replace native date inputs with Calendar popovers and fix layout (d173f48)

**AddTodoRow changes:**
- Changed `dueDate` state from `string` to `Date | undefined`
- Removed `dateInputRef` (no longer needed)
- Added `isDateOpen` boolean state for date popover
- Replaced `<Input type="date">` block with `<Popover>` + `<Calendar mode="single">`
- Trigger shows CalendarIcon + formatted date (`MMM d`) or "Date" placeholder in muted-foreground
- `handleSubmit` now uses `format(dueDate, "yyyy-MM-dd")` from date-fns
- `resetForm` now sets `setDueDate(undefined)`
- Fixed name input padding from `px-1` to `px-2`

**TodoRow changes:**
- Removed `isEditingDate` state
- Added `isDateOpen` boolean state for date popover
- Removed `saveDate`, `handleDateKeyDown`, `handleDateBlur` functions
- Replaced inline date input/button with `<Popover>` + `<Calendar mode="single">`
- Date display shows `format(parseISO(due_date), "MMM d")` with overdue styling preserved
- CalendarIcon shown on `group-hover` when no date is set (same opacity pattern as delete button)
- `onSelect` calls `onUpdate({ id, due_date: date ? format(date, "yyyy-MM-dd") : null })` and closes popover

**index.tsx changes:**
- Added `mx-auto w-full max-w-3xl` to both `TodosPage` and `TodosLoading` main elements

## Deviations from Plan

None — plan executed exactly as written.

## Commits

| Task | Commit  | Description |
|------|---------|-------------|
| 1    | 747f414 | feat(quick-260402-1a5): install shadcn Calendar component with react-day-picker and date-fns |
| 2    | d173f48 | feat(quick-260402-1a5): replace native date inputs with Calendar popovers and fix layout |

## Self-Check: PASSED

- [x] `src/components/ui/calendar.tsx` exists
- [x] `react-day-picker` in package.json
- [x] `date-fns` in package.json
- [x] Commit 747f414 exists
- [x] Commit d173f48 exists
- [x] Build passes with zero errors
