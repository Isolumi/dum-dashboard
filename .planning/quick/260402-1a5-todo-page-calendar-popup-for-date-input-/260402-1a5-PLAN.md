---
phase: quick
plan: 260402-1a5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/calendar.tsx
  - src/components/ui/button.tsx
  - src/routes/_layout/todos/-AddTodoRow.tsx
  - src/routes/_layout/todos/-TodoRow.tsx
  - src/routes/_layout/todos/index.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Clicking the date field in AddTodoRow opens a calendar popup instead of using native date input"
    - "Clicking the date display in TodoRow opens a calendar popup for date editing"
    - "The todo list content area has a sensible max-width so it does not stretch edge-to-edge on wide screens"
    - "Input fields have comfortable padding (no cramped text)"
  artifacts:
    - path: "src/components/ui/calendar.tsx"
      provides: "shadcn Calendar component (react-day-picker)"
    - path: "src/routes/_layout/todos/-AddTodoRow.tsx"
      provides: "Calendar popover for date input in new todo row"
    - path: "src/routes/_layout/todos/-TodoRow.tsx"
      provides: "Calendar popover for date editing in existing todo row"
    - path: "src/routes/_layout/todos/index.tsx"
      provides: "Max-width container for todo page content"
  key_links:
    - from: "src/routes/_layout/todos/-AddTodoRow.tsx"
      to: "src/components/ui/calendar.tsx"
      via: "Calendar import and Popover wrapping"
      pattern: "import.*Calendar.*calendar"
    - from: "src/routes/_layout/todos/-TodoRow.tsx"
      to: "src/components/ui/calendar.tsx"
      via: "Calendar import and Popover wrapping"
      pattern: "import.*Calendar.*calendar"
---

<objective>
Replace native HTML date inputs with shadcn Calendar popover pickers in the todo page (both AddTodoRow and TodoRow), add a max-width container to the todo page layout, and fix input padding.

Purpose: Native date inputs look inconsistent across browsers and clash with the dark theme. A calendar popup provides a polished, consistent date-picking experience. Max-width prevents content from stretching uncomfortably wide on large screens.
Output: Updated -AddTodoRow.tsx, -TodoRow.tsx, index.tsx, plus new calendar.tsx component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/routes/_layout/todos/-AddTodoRow.tsx
@src/routes/_layout/todos/-TodoRow.tsx
@src/routes/_layout/todos/index.tsx
@src/components/ui/popover.tsx
@src/components/ui/button.tsx
@components.json

<interfaces>
<!-- This project uses base-ui (not Radix). Use `render` prop on PopoverTrigger, not `asChild`. -->
<!-- shadcn base style: base-nova. iconLibrary: lucide. aliases: #/components, #/lib, #/hooks -->

From src/components/ui/popover.tsx:
- Popover, PopoverContent, PopoverTrigger (base-ui: use render prop)

From src/lib/database.types.ts:
- Todo type with fields: id, name, priority, status, due_date (string | null), created_at

From src/routes/todos/todos.functions.ts:
- createTodo, updateTodo, deleteTodo, getTodos server functions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Calendar component and dependencies</name>
  <files>src/components/ui/calendar.tsx, src/components/ui/button.tsx</files>
  <action>
    Install the shadcn calendar component which brings in react-day-picker and date-fns:

    ```bash
    bunx --bun shadcn@latest add calendar --overwrite
    ```

    This will:
    1. Create src/components/ui/calendar.tsx (new file)
    2. Update src/components/ui/button.tsx (minor padding tweak on lg size variant -- safe to overwrite)
    3. Install react-day-picker and date-fns as dependencies

    After installation, read the created calendar.tsx to confirm it installed correctly and understand the Calendar component API (mode="single", selected, onSelect props).

    No further modifications needed to these files.
  </action>
  <verify>
    <automated>ls src/components/ui/calendar.tsx && grep "react-day-picker" package.json && grep "date-fns" package.json</automated>
  </verify>
  <done>calendar.tsx exists in ui directory, react-day-picker and date-fns are in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Replace native date inputs with Calendar popovers and fix layout</name>
  <files>src/routes/_layout/todos/-AddTodoRow.tsx, src/routes/_layout/todos/-TodoRow.tsx, src/routes/_layout/todos/index.tsx</files>
  <action>
    **A. AddTodoRow.tsx -- Replace native date input with Calendar popover:**

    1. Add imports: `Calendar` from `#/components/ui/calendar`, `Button` from `#/components/ui/button`, `CalendarIcon` from `lucide-react`, and `format` from `date-fns`.
    2. Change `dueDate` state from `string` to `Date | undefined` (initialized as `undefined`).
    3. Add `isDateOpen` state (boolean, default false) for the date popover.
    4. Remove the `dateInputRef` (no longer needed for a native input).
    5. Replace the date input `<div className="w-24 shrink-0">` block with a Popover containing:
       - A PopoverTrigger using `render` prop (base-ui pattern) wrapping a Button variant="ghost" that displays:
         - CalendarIcon (with `data-icon="inline-start"` per shadcn icon rules)
         - The formatted date if set (`format(dueDate, "MMM d")`) or "Date" as placeholder text in muted-foreground
       - A PopoverContent (align="end", className="w-auto p-0") containing:
         - A Calendar with `mode="single"`, `selected={dueDate}`, `onSelect` that sets the date and closes the popover
    6. Update `handleSubmit` to convert `dueDate` Date to ISO string: `due_date: dueDate ? format(dueDate, "yyyy-MM-dd") : null`.
    7. Update `resetForm` to set `setDueDate(undefined)` instead of `setDueDate("")`.
    8. Update Tab key handling: the priority button's Tab (non-shift) should now focus or open the date popover trigger (find the button ref or just remove the dateInputRef Tab logic since the popover trigger is a standard focusable button).
    9. Keep the keyboard hint and the overall bg-accent/50 rounded-md outer wrapper.

    **B. TodoRow.tsx -- Replace native date input with Calendar popover:**

    1. Add imports: `Calendar` from `#/components/ui/calendar`, `CalendarIcon` from `lucide-react`, `format, parseISO` from `date-fns`.
    2. Remove `isEditingDate` state (no longer needed -- the popover handles open/close).
    3. Add `isDateOpen` state (boolean, default false).
    4. Replace the date `<div className="w-24 shrink-0 text-right">` block with a Popover:
       - PopoverTrigger uses `render` prop on a `<button>` element displaying:
         - If date exists: formatted date text (e.g., `format(parseISO(todo.due_date), "MMM d")`) with overdue styling (text-destructive)
         - If no date: a CalendarIcon in muted-foreground that appears on group-hover (use `opacity-0 group-hover:opacity-100 transition-opacity` like the delete button)
       - PopoverContent (align="end", className="w-auto p-0") containing:
         - Calendar with `mode="single"`, `selected={todo.due_date ? parseISO(todo.due_date) : undefined}`, `onSelect` that calls `onUpdate({ id: todo.id, due_date: date ? format(date, "yyyy-MM-dd") : null })` and closes the popover
    5. Remove `saveDate`, `handleDateKeyDown`, `handleDateBlur` functions (no longer needed).

    **C. index.tsx -- Add max-width container:**

    1. In `TodosPage`, wrap the existing `<main>` content in a max-width constraint. Change the outer `<main>` to:
       ```
       <main className="mx-auto flex w-full max-w-3xl flex-col gap-8 p-6">
       ```
       This centers the content and caps it at 768px (max-w-3xl) so the todo list is comfortable on wide screens.
    2. Apply the same max-width pattern to `TodosLoading` for consistency.

    **D. Input padding fixes in AddTodoRow.tsx:**

    1. On the name Input, change `px-1` to `px-2` for more comfortable text padding.

    **Style notes:**
    - Use semantic tokens only (bg-popover, text-muted-foreground, etc.) -- no hardcoded colors.
    - Use `cn()` for conditional classes.
    - Calendar trigger button should use `shrink-0` to prevent layout compression.
    - The date trigger area in TodoRow should keep `w-24 shrink-0` wrapper for consistent column width.
  </action>
  <verify>
    <automated>cd /Users/isolumi/Documents/CS/dum-dashboard && bun run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - AddTodoRow shows a calendar popup when clicking the date area, displaying selected date as "MMM d" format
    - TodoRow shows a calendar popup when clicking the date display or the calendar icon (for todos without dates)
    - Todo page content is centered with max-w-3xl on wide screens
    - Name input in AddTodoRow has comfortable px-2 padding
    - Build succeeds with no type errors
  </done>
</task>

</tasks>

<verification>
1. `bun run build` completes without errors
2. Visual check: AddTodoRow date field shows calendar icon + popover
3. Visual check: TodoRow date field opens calendar popover on click
4. Visual check: Todo page content does not stretch full-width on wide screens
5. Visual check: Inputs have comfortable padding
</verification>

<success_criteria>
- shadcn Calendar component installed with react-day-picker and date-fns
- Both AddTodoRow and TodoRow use Calendar popovers instead of native date inputs
- Todo page has max-w-3xl centered layout
- Input padding is comfortable (px-2 on name input)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/quick/260402-1a5-todo-page-calendar-popup-for-date-input-/260402-1a5-SUMMARY.md`
</output>
