---
task_id: 260402-13d
type: quick
file: src/routes/_layout/todos/-AddTodoRow.tsx
autonomous: true
---

<objective>
Fix two invisible-input UX regressions in AddTodoRow's expanded creation form:
1. Name input has no visible affordance (no border, no padding) — looks like a bug
2. Date input has no padding/border, collapsing its layout so the browser's native calendar popup is inaccessible

Purpose: A creation form that is always visible in expanded state must communicate "this is editable" at rest, not only on focus.
Output: Both inputs have subtle but visible underline-style styling; date input renders its native calendar icon correctly.
</objective>

<context>
@src/routes/_layout/todos/-AddTodoRow.tsx
@src/components/ui/input.tsx

Key facts:
- Outer wrapper already has `bg-accent/50` — inputs should feel recessed, not heavy
- Input base has `h-8 w-full border border-input bg-transparent px-2.5 py-1` — current overrides strip all of this
- Semantic tokens in use: `border-input`, `ring`, `ring/50`, `muted-foreground` — all available
- CLAUDE.md: NO hardcoded hex/oklch/rgb — Tailwind semantic tokens only
- Constraint: only touch -AddTodoRow.tsx
- TodoRow uses the same borderless style for its *inline edit* mode (click-to-edit), which is correct there but wrong for a persistent creation form
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle name and date inputs with visible underline affordance</name>
  <files>src/routes/_layout/todos/-AddTodoRow.tsx</files>
  <action>
    Apply targeted className changes to both inputs. Use underline (bottom-border) style to stay lightweight against the `bg-accent/50` wrapper.

    **Name input** (line 106) — replace className with:
    ```
    "h-auto flex-1 border-0 border-b border-input/60 px-1 pb-0.5 text-base shadow-none focus-visible:border-ring focus-visible:outline-none focus-visible:ring-0"
    ```
    Rationale: `border-b border-input/60` gives a subtle visible bottom border at rest; `px-1 pb-0.5` gives minimal but real padding so the text isn't flush against the border; `focus-visible:border-ring` transitions to the brand violet on focus (matches ring token); removes the `focus-visible:ring-1 focus-visible:ring-ring/50` ring (not needed with bottom-border style — the border colour change is sufficient focus signal).

    **Date input** (line 188) — replace className with:
    ```
    "h-auto w-full border-0 border-b border-input/60 px-1 py-0.5 text-sm shadow-none focus-visible:border-ring focus-visible:outline-none focus-visible:ring-0"
    ```
    Rationale: Adds `w-full` so the input fills its `w-24` wrapper (previously missing); `py-0.5` gives the browser enough vertical space to render the date field segments and calendar icon; `border-b border-input/60` matches name input treatment; `px-1` provides right-edge clearance for the calendar icon click target.

    No other lines in the file need changing. Do not touch TodoRow.
  </action>
  <verify>
    1. `bun run build` (or `bun run typecheck` if available) exits 0 — no TypeScript errors
    2. Dev server: `bun run dev`, navigate to /todos, click "Add a todo..." to expand AddTodoRow
    3. Name input shows a faint bottom border line at rest (not just on focus)
    4. Clicking the date area opens the native date picker / calendar popup
    5. Both inputs show the violet border on focus (border-ring token = oklch(0.65 0.2 270))
  </verify>
  <done>
    - Name input has visible bottom-border affordance at rest
    - Date input has enough padding that the native calendar icon is within the visible/clickable area
    - Both inputs use only semantic Tailwind tokens (no hardcoded colours)
    - Build passes with no TypeScript errors
  </done>
</task>

</tasks>

<success_criteria>
- `bun run build` exits 0
- Expanded AddTodoRow shows a visible (if subtle) bottom border on both name and date inputs at rest
- Native date calendar popup opens when clicking the date input's calendar icon on macOS/Chrome
- No hardcoded hex/oklch/rgb values introduced
</success_criteria>
