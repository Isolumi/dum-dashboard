---
phase: quick
plan: 260329-3ht
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - CLAUDE.md
  - pnpm-lock.yaml
autonomous: true
requirements: []
must_haves:
  truths:
    - "bun.lock exists and pnpm-lock.yaml does not"
    - "npm run dev starts the dev server without errors"
    - "npm run lint exits 0"
    - "npm run fmt:check exits 0"
    - "CLAUDE.md references bun/bunx, not pnpm"
  artifacts:
    - path: "bun.lock"
      provides: "Bun lockfile"
    - path: "package.json"
      provides: "No pnpm config block"
  key_links:
    - from: "package.json"
      to: "bun.lock"
      via: "bun install"
      pattern: "bun\\.lock"
---

<objective>
Switch the project's package manager from pnpm to bun.

Purpose: Align the actual package manager with the developer's preferred toolchain and remove the pnpm lockfile and config block.
Output: bun.lock, updated package.json (no pnpm block), updated CLAUDE.md (bun/bunx references), verified dev/lint/fmt:check pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@package.json
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove pnpm artifacts and install with bun</name>
  <files>pnpm-lock.yaml, package.json, bun.lock</files>
  <action>
    1. Delete pnpm-lock.yaml: `rm pnpm-lock.yaml`
    2. Remove the `"pnpm"` config block from package.json (lines 55–59: `"pnpm": { "onlyBuiltDependencies": ["esbuild", "lightningcss"] }`). The `onlyBuiltDependencies` hint is pnpm-specific; bun does not use it. Remove the entire block.
    3. Run `bun install` in the project root to generate bun.lock and populate node_modules.
    4. Verify bun.lock was created: `ls bun.lock`
  </action>
  <verify>
    <automated>ls bun.lock && ! ls pnpm-lock.yaml 2>/dev/null && node -e "const p=require('./package.json'); process.exit(p.pnpm ? 1 : 0)"</automated>
  </verify>
  <done>bun.lock exists, pnpm-lock.yaml is gone, package.json has no "pnpm" key</done>
</task>

<task type="auto">
  <name>Task 2: Verify dev server, lint, and format</name>
  <files></files>
  <action>
    Run the three verification commands in sequence:
    1. `bun run lint` — must exit 0 (oxlint passes)
    2. `bun run fmt:check` — must exit 0 (oxfmt finds no formatting issues)
    3. `bun run build` — must complete without errors (confirms node_modules are correct after bun install; use build not dev since dev requires a terminal)

    If lint or fmt:check fail, fix the issue before proceeding (do not skip failures).
    If build fails due to missing type errors or unrelated issues that pre-existed, note them but do not block on pre-existing failures.
  </action>
  <verify>
    <automated>bun run lint && bun run fmt:check</automated>
  </verify>
  <done>lint exits 0, fmt:check exits 0, build completes cleanly</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md pnpm references to bun/bunx</name>
  <files>CLAUDE.md</files>
  <action>
    Read CLAUDE.md and update all pnpm-specific references in the Technology Stack section:

    Changes to make:
    - In the Development Tools table, change the `pnpm` row:
      - Tool: `pnpm` → `bun`
      - Purpose: "Package manager" (unchanged)
      - Notes: Change "Recommended for shadcn/ui CLI (`pnpm dlx shadcn@latest`). Faster installs, strict dependency resolution. All official TanStack Start examples use pnpm." → "Faster installs with bun.lock. Use `bunx --bun shadcn@latest` for the shadcn/ui CLI."
    - In the `@shadcn/ui` row of Core Technologies table, change `pnpm dlx shadcn@latest add` → `bunx --bun shadcn@latest add` and `pnpm dlx shadcn@latest init -t start` → `bunx --bun shadcn@latest init -t start`
    - In the Installation section comments, change any `pnpm` CLI references to `bun`
    - Do NOT change the Alternatives Considered table row where pnpm is listed as the recommendation vs alternatives — that row compares package managers conceptually and pnpm no longer applies; update it to list `bun` as the recommended tool instead.

    The shadcn skill (SKILL.md) already documents `bunx --bun shadcn@latest` as the correct invocation for bun projects — use that form for all shadcn CLI references.

    After edits, the string "pnpm" should not appear in CLAUDE.md except potentially in historical/comparison context.
  </action>
  <verify>
    <automated>grep -c "pnpm" CLAUDE.md || true</automated>
  </verify>
  <done>CLAUDE.md contains no remaining pnpm references (or only in explicit comparison/historical notes); all shadcn CLI commands use `bunx --bun shadcn@latest`</done>
</task>

</tasks>

<verification>
- `ls bun.lock` — lockfile exists
- `! ls pnpm-lock.yaml 2>/dev/null` — old lockfile gone
- `bun run lint` — exits 0
- `bun run fmt:check` — exits 0
- `grep -c "pnpm" CLAUDE.md` — 0 remaining references (or annotated exceptions only)
</verification>

<success_criteria>
- bun.lock present, pnpm-lock.yaml absent
- package.json has no "pnpm" configuration block
- lint and fmt:check both pass with bun
- CLAUDE.md uses bun/bunx throughout, zero stale pnpm references
</success_criteria>

<output>
After completion, create `.planning/quick/260329-3ht-switch-package-manager-from-pnpm-to-bun/260329-3ht-SUMMARY.md`
</output>
