---
phase: quick
plan: 260402-1gu
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/_layout/todos/-AddTodoRow.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "The name input in AddTodoRow expanded state has visible rounded corners"
    - "The name input has a subtle background fill distinguishing it from the row"
    - "The name input has proper padding and border for a polished look"
    - "Focus state shows a ring/border highlight consistent with the design system"
    - "All colours use semantic Tailwind tokens — no hardcoded hex/rgb/oklch values"
  artifacts:
    - path: "src/routes/_layout/todos/-AddTodoRow.tsx"
      provides: "Modern Apple-style text input in AddTodoRow"
      contains: "rounded"
  key_links:
    - from: "src/routes/_layout/todos/-AddTodoRow.tsx"
      to: "src/components/ui/input.tsx"
      via: "Input component import"
      pattern: "import.*Input.*from"
---

<objective>
Restyle the AddTodoRow name input to have a modern, Apple-inspired appearance with rounded corners, proper padding, subtle background fill, and clean border — replacing the current bare underline style.

Purpose: The current input looks unstyled (flat underline, no background, no rounding). A polished input improves the perceived quality of the todo tool.
Output: Updated `-AddTodoRow.tsx` with modern input styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/routes/_layout/todos/-AddTodoRow.tsx
@src/components/ui/input.tsx
@src/styles.css

<interfaces>
<!-- The shadcn Input component's default classes (from src/components/ui/input.tsx): -->
<!-- "h-8 w-full min-w-0 rounded-lg border border-input bg-transparent px-2.5 py-1 text-base transition-colors outline-none ... focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50 ... dark:bg-input/30" -->
<!-- The Input accepts className prop which is merged via cn() — custom classes override defaults. -->

<!-- Current AddTodoRow name input className (line 110): -->
<!-- "h-auto flex-1 border-0 border-b border-input/60 px-2 pb-0.5 text-base shadow-none focus-visible:border-ring focus-visible:outline-none focus-visible:ring-0" -->
<!-- This strips all rounding, background, and border to a bare underline. -->

<!-- Available semantic tokens from styles.css :root: -->
<!-- bg-background, bg-card, bg-input/30, bg-accent, bg-muted, bg-secondary -->
<!-- border-input, border-border, border-ring -->
<!-- text-foreground, text-muted-foreground -->
<!-- ring-ring/50 -->
<!-- rounded-sm/md/lg/xl (mapped to --radius variants) -->

<!-- Project rules (shadcn skill styling.md): -->
<!-- - className for layout, not overriding component colors -->
<!-- - Semantic colors only (bg-primary, text-muted-foreground etc.) -->
<!-- - No hardcoded hex/rgb/oklch values -->
<!-- - Use cn() for conditional classes -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle AddTodoRow name input to modern Apple-inspired look</name>
  <files>src/routes/_layout/todos/-AddTodoRow.tsx</files>
  <action>
In `src/routes/_layout/todos/-AddTodoRow.tsx`, replace the Input's className (line 110) to achieve a modern, Apple-style appearance.

Current className (to replace):
```
"h-auto flex-1 border-0 border-b border-input/60 px-2 pb-0.5 text-base shadow-none focus-visible:border-ring focus-visible:outline-none focus-visible:ring-0"
```

New className:
```
"h-9 flex-1 rounded-lg border border-input/40 bg-input/20 px-3 text-base shadow-none transition-colors placeholder:text-muted-foreground/60 focus-visible:border-ring focus-visible:bg-input/30 focus-visible:ring-2 focus-visible:ring-ring/30"
```

Design rationale for each property:
- `h-9` — Slightly taller than default h-8 for comfortable touch target, Apple-like proportion
- `flex-1` — Fills available horizontal space (unchanged)
- `rounded-lg` — Uses the project's --radius-lg token for smooth Apple-style rounding
- `border border-input/40` — Subtle border at 40% opacity; visible but not heavy. Uses semantic `input` token
- `bg-input/20` — Very subtle dark fill (input token at 20% opacity) so the field reads as a distinct surface against the bg-accent/50 row background
- `px-3` — Comfortable horizontal padding (12px) matching Apple text field proportions
- `text-base` — Keeps current text size (unchanged)
- `shadow-none` — No box shadow for flat Apple aesthetic (unchanged)
- `transition-colors` — Smooth color transitions on focus
- `placeholder:text-muted-foreground/60` — Softer placeholder text at 60% opacity for subtlety
- `focus-visible:border-ring` — On focus, border becomes the ring color (primary violet)
- `focus-visible:bg-input/30` — Slightly stronger background on focus for depth
- `focus-visible:ring-2 focus-visible:ring-ring/30` — Soft focus ring glow at 30% opacity — Apple-style subtle outer glow, not the thick 3px default

All tokens are semantic (input, ring, muted-foreground) — no hardcoded colour values.

IMPORTANT: Only change the className string on the Input element. Do NOT change any other props, event handlers, or other components in the file.
  </action>
  <verify>
    <automated>cd /Users/isolumi/Documents/CS/dum-dashboard && grep -c "rounded-lg" src/routes/_layout/todos/-AddTodoRow.tsx && grep -c "bg-input" src/routes/_layout/todos/-AddTodoRow.tsx && grep -c "border-0" src/routes/_layout/todos/-AddTodoRow.tsx | grep "0"</automated>
  </verify>
  <done>
- The name Input has `rounded-lg` for smooth corners
- The name Input has `bg-input/20` for subtle background fill
- The name Input has `border border-input/40` for a visible but gentle border
- The name Input has a soft focus ring (`ring-2 ring-ring/30`) instead of the previous ring-0
- The old `border-0 border-b` underline pattern is completely removed
- No hardcoded colour values anywhere — all semantic tokens
- All other AddTodoRow functionality (keyboard handling, priority picker, date picker) is unchanged
  </done>
</task>

</tasks>

<verification>
1. `bun run build` completes without errors
2. Visual check: the name input in AddTodoRow (when expanded) shows rounded corners, subtle background, and a soft focus ring
3. `grep -E "(#[0-9a-fA-F]{3,8}|rgb|rgba|hsl|hsla|oklch)" src/routes/_layout/todos/-AddTodoRow.tsx` returns no matches (no hardcoded colours)
</verification>

<success_criteria>
The AddTodoRow name input looks modern and polished — rounded corners, subtle background fill, gentle border, soft focus glow — consistent with Apple's design language while using only the project's semantic colour tokens.
</success_criteria>

<output>
After completion, create `.planning/quick/260402-1gu-todo-page-addtodorow-text-input-modern-a/260402-1gu-SUMMARY.md`
</output>
