---
phase: 04-todo-tool
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [src/routes/todos/todos.functions.ts]
autonomous: true
gap_closure: true
requirements: [TODO-01]
must_haves:
  truths:
    - "getTodos returns an empty array (not null) when no todos exist"
    - "todos.map() never crashes due to null value"
  artifacts:
    - path: "src/routes/todos/todos.functions.ts"
      provides: "Null-safe getTodos server function"
      contains: "data ?? []"
  key_links:
    - from: "src/routes/todos/todos.functions.ts"
      to: "src/routes/_layout/todos/index.tsx"
      via: "getTodos return value consumed by loader"
      pattern: "data \\?\\? \\[\\]"
---

<objective>
Add null coalescing guard to getTodos so it returns `[]` instead of `null` when the Supabase response has no data.

Purpose: The loader passes `null` through to the component, and `todos.map()` crashes with "Cannot read properties of undefined (reading 'map')". This one-line fix unblocks the entire todo page.
Output: `getTodos` always returns `Todo[]`, never `null`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/routes/todos/todos.functions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add null guard to getTodos return value</name>
  <files>src/routes/todos/todos.functions.ts</files>
  <action>
    In `src/routes/todos/todos.functions.ts`, line 37, change:
    ```
    return data;
    ```
    to:
    ```
    return data ?? [];
    ```

    This is the only change needed. The Supabase `.select()` call returns `data: Todo[] | null`. When the table is empty or the response is absent, `data` is `null`. The `?? []` ensures the function always returns an array, matching the `Promise<Todo[]>` return type annotation already present.

    Do NOT change any other lines in this file.
  </action>
  <verify>
    <automated>cd /Users/isolumi/Documents/CS/dum-dashboard && grep -n "data ?? \[\]" src/routes/todos/todos.functions.ts</automated>
  </verify>
  <done>Line 37 of todos.functions.ts returns `data ?? []` instead of `data`. The getTodos function can never return null.</done>
</task>

</tasks>

<verification>
`grep "data ?? \[\]" src/routes/todos/todos.functions.ts` returns a match on the getTodos handler line.
</verification>

<success_criteria>
- getTodos handler returns `data ?? []` (verified by grep)
- No other lines in the file changed
</success_criteria>

<output>
After completion, create `.planning/phases/04-todo-tool/04-05-SUMMARY.md`
</output>
