---
phase: 04-todo-tool
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
gap_closure: true
requirements: [TODO-07]
must_haves:
  truths:
    - "User is informed that .env.local has invalid Supabase credentials"
    - "User sees exact instructions to fix VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY"
  artifacts: []
  key_links: []
---

<objective>
Surface the invalid Supabase credentials in `.env.local` so the user can fix them.

Purpose: The app crashes on load because `.env.local` has a URL missing the `https://` prefix and a placeholder anon key that is not a valid JWT. This is not a code fix — the user must supply their real Supabase project credentials.
Output: User sees diagnostic output and knows exactly what to change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-todo-tool/04-UAT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Fix Supabase credentials in .env.local</name>
  <files>.env.local</files>
  <action>
    Read `.env.local` and display the current values to the user. Explain the two problems:

    1. `VITE_SUPABASE_URL` is currently `ikvfmgnlfjnkwmsalzop.supabase.co` — missing `https://` prefix. Must be `https://ikvfmgnlfjnkwmsalzop.supabase.co` (or the user's actual Supabase project URL).

    2. `VITE_SUPABASE_PUBLISHABLE_KEY` is `sb_publishable_Y2zicJK3h05FDvAhSb4_8Q_LMsben8i` — not a valid Supabase anon key. A real anon key is a JWT starting with `eyJ` (~200 chars). User must copy from: Supabase Dashboard -> Project Settings -> API -> Project API keys -> `anon` `public`.

    Tell the user to edit `.env.local` with corrected values, then restart dev server (`bun run dev`).

    After user confirms, verify by reading `.env.local` again:
    - URL starts with `https://`
    - Key starts with `eyJ` and length > 100 chars

    Then run: `curl -s -o /dev/null -w "%{http_code}" "<SUPABASE_URL>/rest/v1/" -H "apikey: <ANON_KEY>"` (expect 200).
  </action>
  <verify>
    <automated>cd /Users/isolumi/Documents/CS/dum-dashboard && grep "^VITE_SUPABASE_URL=https://" .env.local && grep "^VITE_SUPABASE_PUBLISHABLE_KEY=eyJ" .env.local</automated>
  </verify>
  <done>
    - .env.local VITE_SUPABASE_URL starts with https://
    - .env.local VITE_SUPABASE_PUBLISHABLE_KEY is a valid JWT (starts with eyJ)
    - curl to Supabase REST endpoint returns 200
  </done>
</task>

</tasks>

<verification>
After credentials are fixed, `bun run dev` should start without Supabase connection errors and `/todos` should load (possibly showing empty state if no todos exist).
</verification>

<success_criteria>
- .env.local contains a valid VITE_SUPABASE_URL with https:// prefix
- .env.local contains a valid JWT anon key for VITE_SUPABASE_PUBLISHABLE_KEY
- curl to the Supabase REST endpoint returns 200
</success_criteria>

<output>
After completion, create `.planning/phases/04-todo-tool/04-04-SUMMARY.md`
</output>
