---
phase: 04-todo-tool
plan: 07
type: execute
wave: 1
depends_on: []
files_modified: [src/routes/_layout/todos/-AddTodoRow.tsx]
autonomous: true
requirements: [TODO-01]
gap_closure: true

must_haves:
  truths:
    - "Typing a character on the collapsed AddTodoRow expands it and seeds the name input with that character"
    - "Tab from priority badge moves focus to date picker trigger"
    - "Tab from date picker trigger wraps focus back to name input"
  artifacts:
    - path: "src/routes/_layout/todos/-AddTodoRow.tsx"
      provides: "AddTodoRow with correct focus management"
      contains: "collapsedRowRef"
  key_links:
    - from: "collapsed div"
      to: "onKeyDown handler"
      via: "collapsedRowRef.current?.focus() after mount and after resetForm()"
      pattern: "collapsedRowRef"
    - from: "priority button Tab handler"
      to: "date trigger button"
      via: "dateTriggerRef.current?.focus()"
      pattern: "dateTriggerRef"
---

<objective>
Fix two focus-management bugs in AddTodoRow that prevent type-to-expand on the collapsed row and Tab navigation from the priority badge to the date picker.

Purpose: Close the two remaining UAT failures (tests 7 and 11) so Phase 04 passes all acceptance criteria.
Output: Updated `-AddTodoRow.tsx` with working focus management.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-todo-tool/04-UAT.md
@src/routes/_layout/todos/-AddTodoRow.tsx
</context>

<interfaces>
<!-- Current component structure the executor needs to understand -->

From src/routes/_layout/todos/-AddTodoRow.tsx:
```typescript
// Existing refs (lines 40-41):
const nameInputRef = useRef<HTMLInputElement>(null);
const priorityButtonRef = useRef<HTMLButtonElement>(null);

// Collapsed div (lines 63-82): has tabIndex=0 and onKeyDown, but no ref
// resetForm (lines 43-50): collapses form but does not refocus collapsed row
// Priority button Tab handler (lines 124-127): calls e.preventDefault() then does nothing
// Date picker PopoverTrigger (lines 172-188): uses render prop with <Button>, no ref on the Button
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix focus management for collapsed-row typing and Tab cycle</name>
  <files>src/routes/_layout/todos/-AddTodoRow.tsx</files>
  <action>
Make three targeted changes to the existing AddTodoRow component:

**Fix 1 — Collapsed row never receives focus (UAT test 7):**
- Add `const collapsedRowRef = useRef<HTMLDivElement>(null)` alongside the existing refs (near line 40).
- Attach `ref={collapsedRowRef}` to the collapsed div (the one at line 63 with `tabIndex={0}`).
- Add a `useEffect` that calls `collapsedRowRef.current?.focus()` when `isExpanded` transitions to `false`. The dependency array should be `[isExpanded]`. Guard the focus call: only call `.focus()` when `!isExpanded`. This covers both initial mount (isExpanded starts false) and after resetForm() collapses the row.

**Fix 2 — Tab from priority to date picker (UAT test 11):**
- Add `const dateTriggerRef = useRef<HTMLButtonElement>(null)` alongside the existing refs.
- Attach `ref={dateTriggerRef}` to the `<Button>` inside the date picker's `PopoverTrigger render={...}` prop (currently at line 174). The Button already receives other props; just add the ref.
- In the priority button's `onKeyDown` handler (line 125), replace the dead comment (`// Date popover trigger is a standard focusable button — let Tab flow naturally`) with `dateTriggerRef.current?.focus()`. Keep the existing `e.preventDefault()` — it is correct since we are manually managing focus.

**Fix 3 — Tab from date picker wraps to name input:**
- In the date picker `<Button>`'s `onKeyDown` handler (line 177), add a case for `e.key === "Tab" && !e.shiftKey`: call `e.preventDefault()` then `nameInputRef.current?.focus()`. This completes the Tab cycle: name -> priority -> date -> name.
- Also add Shift+Tab from date picker to priority: when `e.key === "Tab" && e.shiftKey`, call `e.preventDefault()` then `priorityButtonRef.current?.focus()`.

Do NOT change any styling, layout, or other behavior. Only focus management.
  </action>
  <verify>
    <automated>cd /Users/isolumi/Documents/CS/dum-dashboard && bun run dev &>/dev/null & sleep 3 && echo "Dev server started" && kill %1 2>/dev/null; echo "Build check passed"</automated>
  </verify>
  <done>
- The collapsed AddTodoRow div receives focus on mount and after form cancel/submit, so typing a character expands the row and seeds the name input.
- Tab from name input moves to priority badge (already works).
- Tab from priority badge moves focus to the date picker trigger button.
- Tab from date picker trigger wraps focus back to the name input.
- Shift+Tab reverses the cycle correctly.
- No other behavior or styling is changed.
  </done>
</task>

</tasks>

<verification>
After the fix, manually verify in the browser:
1. Load /todos. Without clicking, type a character — the AddTodoRow expands and the character appears in the name input.
2. With AddTodoRow expanded, press Tab repeatedly — focus cycles: name -> priority -> date -> name.
3. Press Shift+Tab — focus cycles in reverse.
4. Press Escape or Enter to submit/cancel — after collapse, typing again immediately expands the row (focus returned to collapsed div).
</verification>

<success_criteria>
UAT tests 7 and 11 both pass on re-test. No regressions in tests 1-6, 8-10, 12-20.
</success_criteria>

<output>
After completion, create `.planning/phases/04-todo-tool/04-07-SUMMARY.md`
</output>
