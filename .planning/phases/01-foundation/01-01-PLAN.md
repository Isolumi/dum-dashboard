---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - components.json
  - src/routes/__root.tsx
  - src/routes/index.tsx
  - src/router.tsx
  - src/routeTree.gen.ts
  - src/styles/app.css
  - src/components/ui/
  - src/lib/utils.ts
  - app.config.ts
autonomous: true
requirements:
  - FOUN-01
  - FOUN-02

must_haves:
  truths:
    - "The project scaffolds and the dev server starts without errors"
    - "shadcn/ui is initialized with base-nova preset for TanStack Start"
    - "oxlint and oxfmt are installed and run without errors on the scaffold"
    - "package.json contains lint, lint:fix, fmt, and fmt:check scripts"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all dependencies and OXC scripts"
      contains: "oxlint"
    - path: "components.json"
      provides: "shadcn/ui configuration for TanStack Start"
      contains: "base-nova"
    - path: "src/routes/__root.tsx"
      provides: "Root route layout importing CSS"
      contains: "createRootRoute"
    - path: "src/styles/app.css"
      provides: "Main CSS entry point with Tailwind and tw-animate-css imports"
      contains: "@import"
  key_links:
    - from: "src/routes/__root.tsx"
      to: "src/styles/app.css"
      via: "CSS import with ?url suffix"
      pattern: "app\\.css\\?url"
    - from: "package.json"
      to: "oxlint"
      via: "devDependencies + scripts"
      pattern: "\"lint\":"
---

<objective>
Scaffold the TanStack Start project, initialize shadcn/ui with the base-nova preset, and configure OXC tooling (oxlint + oxfmt) as dev dependencies with package.json scripts.

Purpose: Establish the runnable project skeleton that all subsequent phases build on. Without this, there is no codebase, no build tooling, no component library foundation.
Output: A working TanStack Start project with shadcn/ui initialized, OXC tooling installed, and the dev server running cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold TanStack Start project and initialize shadcn/ui</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, components.json, src/routes/__root.tsx, src/routes/index.tsx, src/router.tsx, src/styles/app.css, src/lib/utils.ts, app.config.ts</files>
  <read_first>
    - CLAUDE.md (technology stack, version constraints, what NOT to use)
    - .planning/phases/01-foundation/01-RESEARCH.md (scaffold flow, pitfalls, version table)
    - .planning/phases/01-foundation/01-UI-SPEC.md (base-nova preset, token architecture, CSS entry point order)
    - .claude/skills/shadcn/SKILL.md (CLI commands, init workflow)
  </read_first>
  <action>
    **Step 1 — Install pnpm globally** (per RESEARCH.md Pitfall 3 — not present on this machine):
    ```bash
    npm install -g pnpm
    ```

    **Step 2 — Scaffold TanStack Start project** in the current working directory `/Users/isolumi/Documents/CS/dum-dashboard`:

    The project directory already has `.planning/`, `.git/`, `CLAUDE.md`, etc. We need to scaffold the TanStack Start project here. Use the TanStack CLI to create a new project, then move its contents into this directory:
    ```bash
    cd /tmp
    npx @tanstack/cli@latest create --template start --add-on tailwind dum-dashboard
    ```
    If the CLI prompts for options, select: TanStack Start, React, Tailwind CSS. Do NOT select the shadcn add-on (per RESEARCH.md Pitfall 4 — init shadcn separately).

    After creation, copy all scaffolded files (except `.git/`) from `/tmp/dum-dashboard` into `/Users/isolumi/Documents/CS/dum-dashboard/`, preserving existing `.planning/`, `.agents/`, `.claude/`, `CLAUDE.md`, and `skills-lock.json`.

    **Step 3 — Install dependencies:**
    ```bash
    cd /Users/isolumi/Documents/CS/dum-dashboard
    pnpm install
    ```

    **Step 4 — Initialize shadcn/ui** with base-nova preset (per D-01 UI-SPEC, SKILL.md):
    ```bash
    pnpm dlx shadcn@latest init --preset base-nova
    ```
    Accept defaults when prompted. This creates `components.json`, sets up `@theme inline` block in the CSS file, installs `lucide-react` and `tw-animate-css`, and creates `src/lib/utils.ts` with the `cn()` helper.

    **Step 5 — Verify CSS entry point exists:**
    Run `pnpm dlx shadcn@latest info` to check the `tailwindCssFile` path. Record this path — Plan 02 needs it.

    **Step 6 — Generate route tree:**
    ```bash
    pnpm dev
    ```
    Run the dev server briefly to trigger `routeTree.gen.ts` generation (per RESEARCH.md Pitfall 6). Once it compiles successfully and the server starts, stop it (Ctrl+C).

    **Important constraints:**
    - Do NOT install `tailwindcss-animate` (use `tw-animate-css` per CLAUDE.md §What NOT to Use)
    - Do NOT create `tailwind.config.js` (Tailwind v4 uses CSS-first config per CLAUDE.md)
    - Do NOT install Prettier (per D-07, oxfmt only)
    - Do NOT install ESLint (per D-06, oxlint only)
    - Do NOT use `React.forwardRef` anywhere (React 19 per CLAUDE.md)
  </action>
  <verify>
    <automated>cd /Users/isolumi/Documents/CS/dum-dashboard && test -f package.json && test -f components.json && test -f src/routes/__root.tsx && test -f src/routeTree.gen.ts && echo "PASS: All scaffold files exist"</automated>
  </verify>
  <acceptance_criteria>
    - package.json exists and contains `@tanstack/react-start` in dependencies
    - components.json exists and contains `base-nova` or `nova` as the style/preset
    - src/routes/__root.tsx exists and contains `createRootRoute`
    - src/routes/index.tsx exists
    - src/styles/app.css (or equivalent per shadcn info) exists and contains `@import "tailwindcss"`
    - src/lib/utils.ts exists and contains `cn` function export
    - src/routeTree.gen.ts exists (generated by dev server)
    - pnpm-lock.yaml exists
    - No `tailwind.config.js` file exists
    - No `eslint` or `prettier` packages in package.json
  </acceptance_criteria>
  <done>TanStack Start project scaffolded with shadcn/ui initialized using base-nova preset. Dev server starts and compiles without errors. Route tree auto-generated.</done>
</task>

<task type="auto">
  <name>Task 2: Install OXC tooling and add package.json scripts</name>
  <files>package.json</files>
  <read_first>
    - package.json (current state after Task 1 scaffold)
    - .planning/phases/01-foundation/01-RESEARCH.md (OXC versions, scripts, Pitfall 5)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-06 zero-config oxlint, D-07 oxfmt only)
  </read_first>
  <action>
    **Step 1 — Install OXC tools as exact-pin devDependencies** (per RESEARCH.md versions, STATE.md blocker on oxfmt beta):
    ```bash
    cd /Users/isolumi/Documents/CS/dum-dashboard
    pnpm add -D oxlint@1.57.0 oxfmt@0.42.0
    ```
    Use exact-pin versions (no `^` prefix) because oxfmt is beta-status (per STATE.md blockers).

    **Step 2 — Add scripts to package.json.** Edit the `"scripts"` section to add these four entries (do not remove existing scripts like `dev`, `build`, `start`):
    ```json
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check"
    ```

    **Step 3 — Format the scaffold with oxfmt** (per RESEARCH.md Pitfall 5 — oxfmt may disagree with scaffold formatting):
    ```bash
    pnpm fmt
    ```
    This auto-corrects any formatting differences in scaffolded files. It also validates that oxfmt runs without crashing (beta stability check per D-07).

    **Step 4 — Run oxlint** (per D-06 zero-config defaults):
    ```bash
    pnpm lint
    ```
    This validates zero-config linting works. If violations appear on scaffolded code, fix them (they would be scaffold bugs, not project code issues).

    **Step 5 — Verify both checks pass:**
    ```bash
    pnpm lint && pnpm fmt:check
    ```
    Both must exit with code 0. If `oxfmt --check` fails after running `oxfmt`, investigate — it may indicate an oxfmt bug. Document any workarounds.

    **Do NOT:**
    - Create `.oxlintrc.json` (per D-06 zero-config)
    - Create any oxfmt config file (per D-07)
    - Install Prettier as fallback (per D-07 — only add if oxfmt proves unstable)
    - Install ESLint (per D-06)
  </action>
  <verify>
    <automated>cd /Users/isolumi/Documents/CS/dum-dashboard && pnpm lint && pnpm fmt:check && echo "PASS: OXC tooling clean"</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies contains `"oxlint": "1.57.0"` (exact pin, no ^)
    - package.json devDependencies contains `"oxfmt": "0.42.0"` (exact pin, no ^)
    - package.json scripts contains `"lint": "oxlint ."`
    - package.json scripts contains `"lint:fix": "oxlint . --fix"`
    - package.json scripts contains `"fmt": "oxfmt"`
    - package.json scripts contains `"fmt:check": "oxfmt --check"`
    - `pnpm lint` exits with code 0
    - `pnpm fmt:check` exits with code 0
    - No `.oxlintrc.json` file exists
    - No `.prettierrc` or `.eslintrc` file exists
  </acceptance_criteria>
  <done>oxlint 1.57.0 and oxfmt 0.42.0 installed as exact-pin devDependencies. Four scripts (lint, lint:fix, fmt, fmt:check) added to package.json. Both `pnpm lint` and `pnpm fmt:check` exit 0 on the scaffolded project.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `test -f package.json && test -f components.json` -- scaffold files exist
2. `pnpm dev` -- starts without compilation errors (run briefly, then stop)
3. `pnpm lint && pnpm fmt:check` -- OXC tooling passes with zero violations
4. `grep -q "oxlint" package.json && grep -q "oxfmt" package.json` -- tooling installed
5. `test ! -f tailwind.config.js` -- no Tailwind v3 config file
6. `test ! -f .eslintrc*` -- no ESLint config
</verification>

<success_criteria>
- TanStack Start project runs (`pnpm dev` starts without errors)
- shadcn/ui initialized with base-nova preset (`components.json` exists)
- OXC tools installed and passing (`pnpm lint && pnpm fmt:check` exit 0)
- No forbidden tooling installed (no ESLint, no Prettier, no tailwind.config.js)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
